#include "Config.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
  if (argc < 2) {
    cout << argv[0] << " Version " << Lockey_VERSION_MAJOR << "."
         << Lockey_VERSION_MINOR << endl;
  }
  return 0;
}
